#!/usr/bin/env bash
# Configures a new Ubuntu machine to the requirements
# of task 0 of the Load balancer project.

sudo apt-get update
sudo apt-get -y install nginx
sudo ufw allow 'Nginx HTTP'

# Add custom header X-Served-By with hostname to default site config
# First, remove any existing X-Served-By headers to avoid duplicates
sudo sed -i '/add_header X-Served-By/d' /etc/nginx/sites-available/default
# Then add it once after server_name line with the 'always' parameter
sudo sed -i "/server_name _;/a \	add_header X-Served-By \$hostname always;" /etc/nginx/sites-available/default

# Ensure sites-enabled link is correct
if [ ! -L /etc/nginx/sites-enabled/default ]; then
    sudo rm -f /etc/nginx/sites-enabled/default
    sudo ln -s /etc/nginx/sites-available/default /etc/nginx/sites-enabled/default
fi

# Restart Nginx to apply changes
sudo service nginx restart
